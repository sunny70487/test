def add(one, two):
    """just a add function
    Args:
        one (int): input first parameter
        two (int): input two parameter
    Returns:
        int: the add function result
    """
    return one + two